name: AI Code Review

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: AI Review
        uses: mshick/add-pr-comment@v2
        with:
          message: |
            ⚠️ IA review no configurada todavía correctamente.
            (Placeholder funcionando)